## CoreCityOS Documentation Reference

To build latest documentation you need `jazzy`. To install it run:
```shell
sudo gem install jazzy
```
After that run:
```shell
sh serve.sh
```
This will build documentation in folder master.